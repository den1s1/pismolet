# SEO-аудит запуска продакшена Письмолёта

Дата: 2026-07-07  
Раздел: SEO и маркетинг  
Статус: публичная часть прошла первичную подготовку к индексации; начат этап усиления FAQ, перелинковки и контентных кластеров

## 1. Объём проверки

Проверка относится к публичному сайту `pismolet.ru` и статическим файлам в `public_html` ветки `Development`.

Проверены и частично усилены:

- структура URL из `sitemap.xml`;
- `robots.txt`;
- главная страница;
- тарифы;
- страница «Как работает»;
- SEO-лендинги `/bcc-alternative/` и `/excel-email-list/`;
- контакты и юридический раздел;
- наличие базовых `title`, `description`, `canonical`;
- отсутствие очевидных внутренних и тестовых формулировок;
- базовая связка публичного сайта с `app.pismolet.ru` через кнопки входа и регистрации.

## 2. Статус robots.txt

`public_html/robots.txt` открыт для индексации:

```text
User-agent: *
Allow: /

Sitemap: https://pismolet.ru/sitemap.xml
```

Решение: оставлять открытым для продакшена.

## 3. Статус sitemap.xml

`sitemap.xml` содержит основные публичные страницы:

- `/`;
- `/how-it-works/`;
- `/pricing/`;
- `/payment-info/`;
- `/anti-spam/`;
- `/contacts/`;
- `/legal/`;
- `/legal/offer/`;
- `/legal/privacy/`;
- `/legal/rules/`;
- `/legal/refund/`;
- `/legal/data-processing/`;
- `/articles/`;
- опубликованные статьи;
- SEO-посадочные страницы.

Публичные страницы оплаты `/payment/success/` и `/payment/fail/` в sitemap отсутствуют, потому что платёжные URL должны быть реализованы на стороне `app.pismolet.ru`.

## 4. Главная страница

На главной странице есть:

- корректный `title`;
- `description`;
- canonical на `https://pismolet.ru/`;
- базовые OpenGraph-теги;
- ссылки на вход и регистрацию в приложении;
- единое верхнее меню;
- ссылка на favicon;
- подключение общего CSS с cache-busting параметром;
- блоки перелинковки на основные SEO-сценарии;
- FAQ-блок с вопросами про скрытую копию, Excel, 100–1000 адресатов, самостоятельную настройку и рекламные письма.

Рекомендация: следующим этапом добавить `FAQPage` JSON-LD и расширенные OpenGraph/Twitter Card не только на главную, но и на основные посадочные страницы и статьи.

## 5. Страница тарифов

На `/pricing/` зафиксированы:

- `title`;
- `description`;
- canonical;
- финальная тарифная сетка;
- объяснение модели оплаты;
- ссылка на порядок оплаты, условия возврата и оферту;
- CTA на регистрацию;
- FAQ-блок про расчёт стоимости, оплату конкретной отправки и возврат при отказе по модерации.

Тарифы на странице совпадают с текущим продуктовым решением:

- до 299 писем — 1 ₽ / письмо;
- 300–499 писем — 0,90 ₽ / письмо;
- 500–999 писем — 0,80 ₽ / письмо;
- 1000 и более писем — 0,70 ₽ / письмо.

## 6. Страница «Как работает»

На `/how-it-works/` есть:

- простой пошаговый сценарий;
- объяснение проверки списка;
- ссылки на тарифы и порядок оплаты;
- FAQ-блок про минимальный состав базы, момент юридических подтверждений, проверку списка и модерацию после оплаты.

Рекомендация: позже добавить визуальную схему пользовательского пути и JSON-LD `HowTo` или `FAQPage`, если формат страницы будет стабилен.

## 7. SEO-лендинги первого приоритета

### `/bcc-alternative/`

Страница закрывает кластер:

- альтернатива скрытой копии;
- рассылка без скрытой копии;
- как не раскрыть адреса получателей;
- отправить письмо многим клиентам.

Добавлен FAQ-блок про риски скрытой копии, случаи, когда обычной почты достаточно, и отличие Письмолёта.

### `/excel-email-list/`

Страница закрывает кластер:

- рассылка из Excel;
- email-рассылка из таблицы;
- отправить письмо по списку email;
- загрузить список email.

Добавлен FAQ-блок про минимальную таблицу, дубли, ручную вставку списка и оплату только принятых адресов.

## 8. Стоп-слова и признаки незавершённости

По `public_html` ранее проверены поиском:

- `MVP`;
- `test`;
- `deploy`;
- `placeholder`;
- `soon`;
- `localhost`;
- `example.com`;
- `hello@pismolet.ru`;
- `REPLACE_FIELD`.

Критичных совпадений в публичной части не найдено.

## 9. Что не удалось проверить из среды чата

Из среды чата не удалось подтвердить live HTTP-статусы сайта из-за недоступности DNS/внешнего fetch для домена `pismolet.ru`.

Нужно вручную или с сервера выполнить:

```bash
curl -I https://pismolet.ru/
curl -I https://pismolet.ru/robots.txt
curl -I https://pismolet.ru/sitemap.xml
curl -I https://pismolet.ru/pricing/
curl -I https://pismolet.ru/how-it-works/
curl -I https://pismolet.ru/bcc-alternative/
curl -I https://pismolet.ru/excel-email-list/
curl -I https://pismolet.ru/contacts/
curl -I https://pismolet.ru/legal/
curl -I https://pismolet.ru/articles/
```

Ожидаемо:

- `200 OK` для основных страниц;
- `Content-Type: text/html` для HTML-страниц;
- `Content-Type: text/plain` или близкий для `robots.txt`;
- `Content-Type: application/xml`, `text/xml` или близкий для `sitemap.xml`;
- отсутствие редиректов с `https` на `http`.

## 10. Ручные внешние действия после подтверждения live-статусов

1. Добавить сайт в Яндекс.Вебмастер.
2. Добавить сайт в Google Search Console.
3. Подтвердить права на домен или сайт.
4. Отправить `https://pismolet.ru/sitemap.xml` на переобход.
5. Проверить, что `app.pismolet.ru` не индексируется, если приложение пока не должно попадать в поиск.
6. После индексации проверить брендовый запрос `Письмолёт` и основные посадочные URL.

## 11. Найденные SEO-долги

Не блокируют запуск, но нужны следующим этапом:

1. Добавить OpenGraph/Twitter Card на все основные страницы, статьи и посадочные.
2. Добавить JSON-LD: `Organization`, `WebSite`, `Article`, `BreadcrumbList`, при необходимости `FAQPage`.
3. Добавить FAQ-блоки на `/no-domain-setup/`, `/for-small-business/`, `/for-nko/`, `/for-events/` и ключевые статьи.
4. Расширить контентный кластер статей: скрытая копия, Excel, 100–1000 адресатов, НКО, мероприятия.
5. Провести браузерную проверку мобильной версии после деплоя.
6. Проверить фактические 404/редиректы live-сайта.
7. Проверить индексацию `app.pismolet.ru` и при необходимости закрыть её отдельным `robots.txt`/noindex.

## 12. Контент-план следующей волны

Приоритетные статьи:

1. `Как отправить письмо всем клиентам и не раскрыть адреса`.
2. `Почему не стоит делать рассылку через скрытую копию`.
3. `Как подготовить Excel-таблицу для email-рассылки`.
4. `Что делать, если нужно отправить письмо 100–1000 получателям`.
5. `Email-рассылка для НКО: как написать волонтёрам и участникам`.
6. `Email-рассылка участникам мероприятия: напоминания, изменения и материалы после события`.

Для каждой статьи нужен:

- один основной запрос;
- 3–5 смежных long-tail запросов;
- ссылка на соответствующий лендинг;
- CTA на регистрацию;
- блок про собственную законную базу;
- внутренняя ссылка на `/pricing/` или `/payment-info/` там, где уместно.

## 13. Итог

Критичных SEO-блокеров в статической части сайта не найдено. Первый практический SEO-проход после подготовки к Prodamus выполнен: добавлены FAQ-стили и FAQ-блоки на главную, тарифы, страницу «Как работает», лендинг скрытой копии и лендинг Excel-рассылки. Следующий этап — FAQ и расширение контента на сегментных лендингах и статьях, затем структурированные данные и подключение сайта к поисковым кабинетам после live-проверки.
